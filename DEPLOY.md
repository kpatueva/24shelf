# 📦 Инструкция по развертыванию сайта на сторонний хостинг

Эта инструкция поможет вам развернуть сайт на сторонний хостинг (Netlify, Vercel, или другой) так, чтобы форма заявок работала корректно.

## ⚠️ Важно понять

Форма на сайте сохраняет данные в базу данных **Supabase**. Для работы формы необходимо, чтобы сайт мог подключиться к этой базе данных. Подключение происходит через **переменные окружения** (environment variables).

Когда вы выгружаете сайт на хостинг, файл `.env` **не попадает** на сервер (это сделано специально для безопасности). Поэтому нужно **вручную** добавить эти переменные в настройках вашего хостинга.

---

## 🔑 Шаг 1: Найдите ваши ключи Supabase

Откройте файл `.env` в корне проекта. Вы увидите две переменные:

```
VITE_SUPABASE_URL=https://paoubdkmkldahmgydqrr.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InBhb3ViZGtta2xkYWhtZ3lkcXJyIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzA4MDI0MzAsImV4cCI6MjA4NjM3ODQzMH0.2HCLTmZl20kGBo24onWfNK7CM8uRuuykrlzlr7KOsR0
```

**Скопируйте эти значения** - они понадобятся на следующем шаге.

---

## 🚀 Шаг 2: Выберите ваш хостинг и следуйте инструкции

### Вариант A: Netlify

1. **Зарегистрируйтесь** на [netlify.com](https://www.netlify.com/)

2. **Создайте новый сайт:**
   - Нажмите "Add new site" → "Deploy manually"
   - Перетащите папку `dist` из вашего проекта в окно загрузки
   - Дождитесь завершения загрузки

3. **Добавьте переменные окружения:**
   - Перейдите в настройки сайта: "Site configuration" → "Environment variables"
   - Нажмите "Add a variable"
   - Добавьте первую переменную:
     - **Key:** `VITE_SUPABASE_URL`
     - **Value:** `https://paoubdkmkldahmgydqrr.supabase.co`
   - Нажмите "Create variable"
   - Повторите для второй переменной:
     - **Key:** `VITE_SUPABASE_ANON_KEY`
     - **Value:** `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InBhb3ViZGtta2xkYWhtZ3lkcXJyIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzA4MDI0MzAsImV4cCI6MjA4NjM3ODQzMH0.2HCLTmZl20kGBo24onWfNK7CM8uRuuykrlzlr7KOsR0`

4. **Пересоберите проект:**
   - Вернитесь в проект на компьютере
   - Откройте терминал и выполните команду:
     ```bash
     npm run build
     ```
   - Снова перетащите папку `dist` на Netlify (это обновит сайт)

5. **Готово!** Форма должна работать.

---

### Вариант B: Vercel

1. **Зарегистрируйтесь** на [vercel.com](https://vercel.com/)

2. **Создайте новый проект:**
   - Нажмите "Add New" → "Project"
   - Выберите "Import Git Repository" (если используете Git)
   - Или выберите "Deploy" и загрузите папку проекта

3. **Настройте переменные окружения ДО деплоя:**
   - В разделе "Configure Project" найдите "Environment Variables"
   - Добавьте первую переменную:
     - **Name:** `VITE_SUPABASE_URL`
     - **Value:** `https://paoubdkmkldahmgydqrr.supabase.co`
   - Добавьте вторую переменную:
     - **Name:** `VITE_SUPABASE_ANON_KEY`
     - **Value:** `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InBhb3ViZGtta2xkYWhtZ3lkcXJyIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzA4MDI0MzAsImV4cCI6MjA4NjM3ODQzMH0.2HCLTmZl20kGBo24onWfNK7CM8uRuuykrlzlr7KOsR0`

4. **Нажмите "Deploy"** и дождитесь завершения

5. **Готово!** Форма должна работать.

---

### Вариант C: Другой хостинг (cPanel, обычный хостинг и т.д.)

1. **Соберите проект локально:**
   ```bash
   npm run build
   ```
   Это создаст папку `dist` со всеми файлами сайта.

2. **Загрузите файлы:**
   - Подключитесь к хостингу через FTP (FileZilla или другой FTP-клиент)
   - Загрузите все файлы из папки `dist` в корневую папку сайта (обычно `public_html` или `www`)

3. **⚠️ ПРОБЛЕМА:** На обычном хостинге переменные окружения работают по-другому!

   **Решение:** Вам нужно "зашить" переменные прямо в код перед сборкой.

   - Откройте файл `src/lib/supabase.ts`
   - Замените строки 3-4:

     Было:
     ```typescript
     const supabaseUrl = import.meta.env.VITE_SUPABASE_URL;
     const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY;
     ```

     Стало:
     ```typescript
     const supabaseUrl = 'https://paoubdkmkldahmgydqrr.supabase.co';
     const supabaseAnonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InBhb3ViZGtta2xkYWhtZ3lkcXJyIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzA4MDI0MzAsImV4cCI6MjA4NjM3ODQzMH0.2HCLTmZl20kGBo24onWfNK7CM8uRuuykrlzlr7KOsR0';
     ```

   - Сохраните файл
   - Снова выполните `npm run build`
   - Загрузите новые файлы из `dist` на хостинг

4. **Готово!** Форма должна работать.

---

## 🧪 Проверка работы формы

После деплоя проверьте работу формы:

1. Откройте ваш сайт в браузере
2. Прокрутите вниз до формы "Запросите демонстрацию"
3. Заполните все поля:
   - Имя (обязательное)
   - Email (обязательное)
   - Телефон (необязательное)
   - Компания (необязательное)
4. Нажмите "Отправить заявку"

**Что должно произойти:**
- Форма покажет сообщение "Спасибо! Ваша заявка успешно отправлена"
- Поля формы очистятся
- Данные сохранятся в базу данных Supabase

**Если что-то пошло не так:**
- Откройте консоль браузера (F12 → вкладка Console)
- Попробуйте отправить форму снова
- Посмотрите на ошибки в консоли
- Проверьте, правильно ли вы добавили переменные окружения

---

## 🔍 Где посмотреть отправленные заявки?

Все заявки сохраняются в базе данных Supabase:

1. Перейдите на [supabase.com](https://supabase.com/)
2. Войдите в аккаунт
3. Откройте проект (URL проекта: https://paoubdkmkldahmgydqrr.supabase.co)
4. В левом меню выберите "Table Editor"
5. Откройте таблицу `lead_requests`
6. Здесь вы увидите все отправленные заявки с именем, email, телефоном и компанией

---

## ❓ Частые проблемы и решения

### Проблема 1: "Missing Supabase environment variables"
**Причина:** Переменные окружения не настроены на хостинге.
**Решение:** Вернитесь к Шагу 2 и добавьте переменные в настройках хостинга.

### Проблема 2: Форма отправляется, но данные не сохраняются
**Причина:** Возможно, неправильные права доступа в базе данных (RLS политики).
**Решение:** Проверьте, что в Supabase включены правильные политики доступа к таблице `lead_requests`.

### Проблема 3: CORS ошибка
**Причина:** Supabase блокирует запросы с вашего домена.
**Решение:** В настройках Supabase добавьте ваш домен в список разрешенных (Authentication → URL Configuration → Site URL).

### Проблема 4: На Netlify/Vercel пересборка не помогает
**Причина:** Возможно, вы добавили переменные ПОСЛЕ первой сборки.
**Решение:**
1. Добавьте переменные окружения
2. Сделайте "Redeploy" (пересборку) сайта в панели хостинга
3. Или загрузите заново файлы из `dist`

---

## 📝 Дополнительная информация

### Для разработчиков

Если вы хотите использовать Git и автоматический деплой:

1. Создайте репозиторий на GitHub/GitLab
2. Загрузите туда проект (НЕ загружайте файл `.env`!)
3. Подключите репозиторий к Netlify/Vercel
4. Добавьте переменные окружения в настройках хостинга
5. При каждом коммите сайт будет автоматически обновляться

### Безопасность

- ⚠️ НИКОГДА не загружайте файл `.env` в публичный репозиторий на GitHub!
- ✅ `ANON_KEY` безопасно показывать в коде - это публичный ключ для клиентских запросов
- ❌ НЕ публикуйте `SERVICE_ROLE_KEY` - это секретный ключ с полным доступом к базе

---

## 🆘 Нужна помощь?

Если у вас что-то не получается:

1. Проверьте консоль браузера (F12) на наличие ошибок
2. Убедитесь, что переменные окружения добавлены правильно (нет опечаток!)
3. Попробуйте очистить кеш браузера (Ctrl+F5)
4. Проверьте, что ваш домен добавлен в настройках Supabase

---

**Удачи с деплоем! 🚀**
